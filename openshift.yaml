project-templates:
  openshift:
    branches:
    - master
    gitweb: http://github.com/openshift/%(name)s/commit/%%(sha)s
    uri: http://github.com/openshift/%(name)s
projects:
  openshift:
    description: The openshift Github organization
    repos:
      angular-extension-registry:
        template: openshift
      angular-key-value-editor:
        template: openshift
      ansible-service-broker:
        template: openshift
      aos-cd-jobs:
        template: openshift
      api:
        template: openshift
      basic-authentication-provider-example:
        template: openshift
      cakephp-ex:
        template: openshift
      client-go:
        template: openshift
      cloud-costs:
        template: openshift
      commons.openshift.org:
        template: openshift
      dancer-ex:
        template: openshift
      devcenter:
        template: openshift
      discoverycenter:
        template: openshift
      django-ex:
        template: openshift
      docker-registry-extensions:
        template: openshift
      dockerexec:
        template: openshift
      geard:
        template: openshift
      generic-admission-server:
        template: openshift
      golang-ex:
        template: openshift
      httpd-ex:
        template: openshift
      imagebuilder:
        template: openshift
      incremental-app:
        template: openshift
      jboss-as7-modules:
        template: openshift
      jboss-eap6-modules:
        template: openshift
      jee-ex:
        template: openshift
      jenkins:
        template: openshift
      jenkins-client-plugin:
        template: openshift
      jenkins-cloud-plugin:
        template: openshift
      jenkins-openshift-login-plugin:
        template: openshift
      jenkins-plugin:
        template: openshift
      kube-projects:
        template: openshift
      kubernetes-namespace-reservation:
        template: openshift
      library:
        template: openshift
      new-hire-tools:
        template: openshift
      object-describer:
        template: openshift
      online-analytics:
        template: openshift
      online-archivist:
        template: openshift
      online-hibernation:
        template: openshift
      oo:
        template: openshift
      oo-index:
        template: openshift
      open-service-broker-sdk:
        template: openshift
      openldap:
        template: openshift
      openshift-ansible:
        template: openshift
      openshift-ansible-contrib:
        template: openshift
      openshift-cns-testdrive:
        template: openshift
      openshift-docs:
        template: openshift
      openshift-extras:
        template: openshift
      openshift-heat-templates:
        template: openshift
      openshift-java-client:
        template: openshift
      openshift-jee-sample:
        template: openshift
      openshift-logos-icon:
        template: openshift
      openshift-origin-design:
        template: openshift
      openshift-pep:
        template: openshift
      openshift-restclient-java:
        template: openshift
      openshift-restclient-python:
        template: openshift
      openshift-sdn:
        template: openshift
      openshift-tools:
        branches:
        - prod
        template: openshift
      openshift.github.com:
        template: openshift
      openshift.org:
        template: openshift
      openshift_zabbix:
        template: openshift
      origin:
        template: openshift
      origin-aggregated-logging:
        template: openshift
      origin-apiman:
        template: openshift
      origin-ci-tool:
        template: openshift
      origin-gce:
        template: openshift
      origin-integration-common:
        template: openshift
      origin-kibana:
        template: openshift
      origin-metrics:
        template: openshift
      origin-server:
        template: openshift
      origin-web-catalog:
        template: openshift
      origin-web-common:
        template: openshift
      origin-web-console:
        template: openshift
      outreachy:
        template: openshift
      php-ex:
        template: openshift
      prometheus-alert-buffer-client-ruby:
        template: openshift
      python-interface:
        template: openshift
      rails-ex:
        template: openshift
      registry-image-widgets:
        template: openshift
      release:
        template: openshift
      release-library:
        template: openshift
      request-header-saml-service-provider:
        template: openshift
      rhc:
        template: openshift
      ruby-20-centos:
        template: openshift
      ruby-ex:
        template: openshift
      ruby-hello-world:
        template: openshift
      simple-openshift-sinatra-sti:
        template: openshift
      source-to-image:
        template: openshift
      sprint_tools:
        template: openshift
      svt:
        template: openshift
      test-maven-app:
        template: openshift
      test-multicast:
        template: openshift
      test-pull-requests:
        template: openshift
      test-pull-requests-redshirt:
        template: openshift
      training:
        template: openshift
      vagrant-openshift:
        template: openshift
